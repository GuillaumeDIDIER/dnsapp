<h1><%= @title %></h1>

<%= form_for(@privileged_user) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit "Modifier" %>
  </div>
<% end %>

<p>
  <%= link_to "Retour au profil de #{@privileged_user.name}", @privileged_user %>
</p>
