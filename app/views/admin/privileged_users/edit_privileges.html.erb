<h1><%= @title %></h1>

<%= form_for(@admin_privileged_user, :url => { :action => :update_privileges } ) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <%= render 'privileges_fields', :f => f %>
  <div class="actions">
    <%= f.submit "Modifier" %>
  </div>
<% end %>
