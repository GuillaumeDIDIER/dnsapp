<h1><%= @title %></h1>

<%= form_for(@admin_privileged_user) do |f| %>
  <%= render 'fields', :f => f %>
  <%= render 'privileges_fields', :f => f %>
  <div class="actions">
    <%= f.submit "Modifier" %>
  </div>
<% end %>
