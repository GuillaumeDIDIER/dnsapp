<h1><%= @title %></h1>

<p>
  <strong>Nom</strong> <%= @zone.name %><br />
  <strong>Zone</strong> <%= @zone.zone %>
</p>

<p>
  <%= link_to "Retour à la liste des zones", admin_zones_path %> |
  <%= link_to "Modifier", edit_admin_zone_path(@zone) %> |
  <%= link_to "Supprimer", @zone,
                           :method => :delete,
                           :confirm => "Supprimer #{@zone.name} ?",
                           :title => "Supprimer #{@zone.name}" %>
</p>
