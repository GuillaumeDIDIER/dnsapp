<h1><%= @title %></h1>

<%= render 'shared/dns_search_form', :object => { :search_path => admin_domain_names_path } %>

<%= will_paginate %>

<ul class="dns">
  <% @domain_names.each do |idns| %>
    <li>
      Name: <%= idns.name %> ttl: <%= idns.ttl %>
      rdtype: <%= idns.rdtype %> rdata: <%= idns.rdata %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
