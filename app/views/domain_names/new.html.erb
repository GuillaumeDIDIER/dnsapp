<h1><%= @title %></h1>

<%= form_for(@domain_name) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit "Nouveau nom" %>
  </div>
<% end %>

<p>
  <%= link_to "Retour à la liste de DNS", domain_names_path %>
</p>
